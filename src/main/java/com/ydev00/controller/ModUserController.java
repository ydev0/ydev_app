package com.ydev00.controller;

import spark.Route;

public class ModUserController extends UserController{

    public Route delete = (request, response) -> {
        return "carlos";
    };

    public Route update = (request, response) -> {
        return "carlos";
    };
}
